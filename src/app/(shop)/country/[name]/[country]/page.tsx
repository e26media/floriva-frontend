import CountryBestSellers from '@/components/CountryBestSellers'

// No props needed — reads /country/australia param automatically
export default function CountryPage() {
  return <CountryBestSellers />
}